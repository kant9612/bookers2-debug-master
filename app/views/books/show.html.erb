<div class='container'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'users/info',user: @bookdetail.user %>
      <h2 class="mt-3">New book</h2>
      <%= render 'form', book: @book %>
    </div>
    <div class='col-md-8 offset-md-1'>
  		<h2>Book detail</h2>
  		<table class='table'>
  		  <tr>
  		    <td><%= link_to user_path(@bookdetail.user) do %>
            <%= attachment_image_tag(@bookdetail.user, :profile_image, :fill, 100, 100, fallback: "no-image-icon.jpg") %><br>
            <%= @bookdetail.user.name %>
            <% end %>
          </td>
          <td><%= link_to @bookdetail.title, book_path(@bookdetail.id) %></td>
          <td><%= @bookdetail.body %></td>
          <% if @bookdetail.user_id == current_user.id %>
          <td><%= link_to 'Edit', edit_book_path(@bookdetail.id), class: "btn btn-sm btn-success edit_book_#{@bookdetail.id}" %></td>
          <td><%= link_to 'Destroy', book_path(@bookdetail.id), method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger destroy_book_#{@bookdetail.id}"%></td>
          <% end %>
        </tr>
      </table>
    </div>
  </div>
</div>